# 正式第一版升级记录

2026-09-21：原项目为 React 19 / Vite 8 / TypeScript，无后端。`abcrm` localStorage 存客户，账号明文写在浏览器代码中；跟进、设置和团队含占位实现。旧浏览器数据不删除、不自动导入。保留深灰侧栏与金色品牌视觉。

实施：Express + PostgreSQL 参数化 SQL、增量迁移、服务端数据库会话、scrypt 密码哈希、CSRF、角色/负责人范围限制、软删除、乐观锁与幂等写入。提供旧数据备份/预检/人工核对导入、Docker+Caddy HTTPS、备份和独立恢复演练。

最终结果：lint、严格类型检查、生产构建、4项单元测试和11组真实数据库/API/浏览器综合测试通过。已实际验证三账号权限、停用会话、独立浏览器上下文和重启持久化、时区日期、冲突、恢复历史、导入去重、手机尺寸业务流程，以及独立空库恢复与备份恢复CLI。

实际证据和环境边界见 [验收记录](ACCEPTANCE.md)。公司公网HTTPS、目标服务器Docker和实物设备验收尚待提供部署条件；不宣称已经上线。真实业务库无演示账号，旧浏览器数据保持不变。
